package com.caseo.domain.model;

public record Asf(
        int id,
        String fullName,
        String fullNameGen,
        String shortName,
        String email,
        String statusShort,
        String arrivalTime
) {
}